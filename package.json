{
  "name": "topsecretportfolio",
  "version": "1.0.0",
  "description": "TODO",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "build-watch": "nodemon --watch './**/*' --ignore 'build/*' --exec 'npm run build'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "markdown-it": "^14.1.0",
    "miniserve": "^0.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
